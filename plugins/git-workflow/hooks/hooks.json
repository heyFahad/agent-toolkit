{
  "$schema": "https://www.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit:*)",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/commit-review-gate.sh",
            "timeout": 15
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(gh pr create:*)",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/pre-staging-conflict-check.sh",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
